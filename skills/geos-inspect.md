---
name: geos-inspect
description: Describe what a GEOS XML file contains — solvers, mesh, materials, BCs.
---

Load and summarize the contents of a GEOS XML file.

## Tools
- `load_xml(file_path)` — parse the file
- `preview_xml(doc_id, section)` — show specific sections
- `describe_element(element_name)` — explain what an element does
